import { FormCard, LoginForm } from '@/components';

const LoginPage = () => {
  return (
    <FormCard title="Вход" description="Въведете данните си по-долу">
      <LoginForm />
    </FormCard>
  );
};

export { LoginPage };
